const tabledata = {
  style: {
  	paddingTop: '2px',
  	paddingBottom: '2px',
  	fontSize: '0.85rem',
  	lineHeight: '1.2rem'
  }
}

const filterrow = {
  style: {
  	visibility: 'hidden',
  	maxHeight: '0px'
  }
}

const pagination = {
  style: {
    maxHeight: '2.3rem',
    fontSize: '0.9rem'
  }
}

const headerrow = {
  style: {
    fontStyle: 'bold'
  }
}

const list = { 
  	fontSize: '0.85em',
  	paddingLeft: '3rem'
}

export default { tabledata, filterrow, list, pagination, headerrow }
